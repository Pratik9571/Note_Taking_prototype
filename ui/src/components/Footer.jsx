import { Box, Typography } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Box>
      <Typography
        variant="h6"
        style={{ margin: "0", minHeight: "0vh", minWidth: "0vw" }}
      >
        Copyright @ Pratik
      </Typography>
    </Box>
  );
};

export default Footer;
